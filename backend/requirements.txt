# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.12
websockets==12.0

# --- Database ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
psycopg2-binary==2.9.9
alembic==1.13.0

# --- Config & Security ---
pydantic==2.9.0
pydantic-settings==2.5.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==43.0.0

# --- Storage & Queue ---
boto3==1.35.0
redis==5.1.0
celery[redis]==5.4.0

# --- HTTP client ---
httpx==0.27.0
tenacity==9.0.0
prometheus-client==0.20.0
fpdf2==2.8.2

# --- Phase 2: ML Pipeline ---
# ASR
faster-whisper==1.0.3

# VAD (Silero uses torch)
torch==2.3.1
torchaudio==2.3.1
onnxruntime==1.19.2

# Speaker Diarization
pyannote.audio==3.3.2

# Audio utilities
soundfile==0.12.1
numpy==1.26.4

# --- Testing ---
pytest==8.3.0
pytest-asyncio==0.24.0

# --- Phase 4: Security & Compliance ---
# MFA / TOTP
pyotp==2.9.0
# spaCy NER for PII redaction (optional – install model separately)
# spacy==3.7.4  # uncomment to enable NER-based redaction
# python -m spacy download en_core_web_sm
